<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <ul>
      <% for (message of messages) { %>
      <li><%= message.user %> : <%= message.text %> [<%= message.added %>]</li>
      <% } %>
    </ul>
    <a href="/new">Add Message</a>
  </body>
</html>
